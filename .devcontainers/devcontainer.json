{
  "name": "Ubuntu + Bun",
  "image": "vikshan/ubuntu-bun:latest",

  // Add the features you want to include in the dev container.
  "features": {
    "ghcr.io/devcontainers/features/github-cli:1": {
      "installDirectlyFromGitHubRelease": true,
      "version": "latest"
    },
    "ghcr.io/devcontainers/features/sshd:1": {
      "version": "latest"
    }
  },

  // Configure tool-specific properties.
  "customizations": {
    "vscode": {
      "extensions": [
        "eamodio.gitlens",
        "WakaTime.vscode-wakatime",
        "coderabbit.coderabbit-vscode"
      ]
    }
  },

  // Use 'postCreateCommand' to run commands after the container is created.
  "postCreateCommand": "[ -f scripts/iamvikshan.sh ] && chmod +x scripts/iamvikshan.sh; bun install",

  // Set `remoteUser` to `root` to connect as root.
  "remoteUser": "root"
}
